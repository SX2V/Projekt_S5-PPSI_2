export enum UserRoleEnum {
  User = 0,
  Admin = 1
}

export enum MatchRequestStatusEnum {
  Pending = 0,
  Accepted = 1,
  Rejected = 2
}

export enum TrainingRequestStatusEnum {
  Pending = 0,
  Accepted = 1,
  Rejected = 2
}
